# Generate SESSION_SECRET with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

# Email of the first admin user.
# Register via the app first, then restart the container to promote.
ADMIN_EMAIL=
